import styled from 'styled-components';

const DesktopStyles = styled.div`
    position: relative;
    display: flex;
    width: 100vw;
    max-width: 100%;
    height: 100vh;
    max-height: 100%; 
`;

export default DesktopStyles;